# Implementation Plans

Generated by the improve skill on 2026-08-09 against commit `cb399a6`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Restore verification baseline + CI gate | P1 | S | — | DONE (2026-08-09, commit 12c16d8) |
| 002  | Test infra: shared settings fixture, obsidian mock enum fix, jest alias fix | P1 | S | 001 | DONE (2026-08-09, commit c2d5900; note: mock_obsidian.ts extension landed here — needed by requireActual — plan 005's mock step is pre-landed) |
| 003  | Characterization tests for frontmatter/variable/date utils | P1 | S–M | 001, 002 | DONE (2026-08-09, commit 73a724f; executor uncovered + fixed a real defect in 002's requireActual mock — enum provided directly now; also recorded that {{DATE-2}} negative offsets are NOT substituted by the current regex) |
| 004  | IGDB auth path tests (token cache, refresh, 401) | P1 | S | 001 | DONE (2026-08-09, commit 478b091; note: the 401-retry test PASSES at 004 — the mocked apiRequest bypasses base_api (plan 006 documented this), so it proves the retry branch; the real dead-code fix is proven by 006's own base_api unit test) |
| 005  | Extend obsidian mock + main.ts orchestration tests | P2 | M | 001, 002 | DONE (2026-08-09, commit 3b3e1de; 6 getRenderedContents cases; mock already fully present from 002 — no additions needed) |
| 007  | Fix frontmatter/variable rendering corruption | P2 | S | 003 | DONE (2026-08-09, commit e45e18b; $-literal replacer, unknown placeholders preserved, \" escaping, newline truncation; 60/60 green) |
| 006  | Fix dead 401-retry in IGDB search | P1 | S | 001, 004 | DONE (2026-08-09, commit 2ab0c0e; throw:false + 4 base_api contract tests; 52/52 green) |
| 008  | Prevent silent overwrite of cover/screenshot files | P2 | S | 001 | DONE (2026-08-09, commit 3cc5233; resolveUniquePath -2/-3 suffix, ext-less handled; ordering confirmed: images download before render, suffixed path embedded; 65/65) |
| 009  | Fix modal dismissal leaving promises pending | P2 | S | 001 | DONE (2026-08-09, commit a7946ff; cancelled/delivered flags in both modals + guards in createNewGameNote/insertMetadata; 6 new tests incl. double-close and in-flight cancel; 71/71) |
| 010  | Remove fork dead code, dead deps, contradictory config | P2 | S | 001 | DONE (2026-08-09, commit fccd1fd; -528 lines: languages.ts, isISBN, {{author}}, popper/ts-node/gh-pages, deploy:pages, .npmrc; tagPrefix in .versionrc; editorconfig spaces/2; noImplicitAny on + 11 typed fixes in utils.ts; 71/71, build OK) |
| 011  | Harden release CI (sunset runner) | P2 | S | 001 | DONE (2026-08-09, commit 439af63; ubuntu-latest; actions all @v4) |
| 012  | Document Insert metadata + add Development section (3 READMEs) | P3 | S | — | DONE (2026-08-09, commit 5289ca0; +13 lines per README; note: "pnpm release pushes a tag" slightly overstates — standard-version doesn't push) |
| 013  | Expose unreachable frontmatter/content settings in UI | P2 | S | 001, 007 | DONE (2026-08-09, commit 0d7c2a4; Note content section: 4 controls incl. addTextArea x2, dropdown disable+re-render; README rows x3; 71/71) |
| 014  | i18n design spike for plugin UI strings | P3 | M | — | DONE (2026-08-09, commit 7df2e05; docs/design/i18n.md: ~50 strings/5 surfaces, mechanism = Obsidian getLanguage() + typed locale maps, PoC t() wired into search modal; 74/74) |
| 015  | Add IGDB similar_games to GameEntry + template | P3 | S–M | 001 | DONE (2026-08-09, commit 21076a6; similar_games.name in fields, GameEntry pair, tests + README rows x3; 75/75) |
| 016  | Fix {{DATE-2}} negative offset silently failing in file-name dates | P1 | S | — | TODO (approved 2026-08-09) |
| 017  | Fix {{time}} returning a date + add template-engine tests | P2 | S | — | TODO (approved 2026-08-09) |
| 018  | Quote truncated frontmatter values so newline handling never emits invalid YAML | P2 | S | — | TODO (approved 2026-08-09) |
| 019  | Close the resolveUniquePath check-then-act race on concurrent image saves | P3 | S | — | TODO (approved 2026-08-09) |
| 020  | Unify locale detection into one shared helper | P3 | S | — | TODO (approved 2026-08-09) |
| 021  | Add request timeouts so hung network calls cannot block the UI forever | P2 | M | — | TODO (approved 2026-08-09) |
| 022  | Implement UI localization per the i18n design spike | P3 | M | 014 | TODO (approved 2026-08-09) |
| 023  | Repo-ify the E2E harness — headless Obsidian smoke tests in e2e/ | P3 | M | — | TODO (approved 2026-08-09) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- Round 2 (016-023): 016+017 both touch date behavior but different files (utils.ts vs template.ts) — run either order; 018 depends on nothing (extends 007's tests); 020 (locale unify) should land BEFORE 022 (i18n implementation) so 022 builds on one detector; 022 depends on 014's design doc; 023 independent. 021 independent.
- Round 1: 001 first: nothing verifiable until `pnpm install` works (node_modules absent at plan time) and CI gates exist.
- 002 before 003/005: the shared settings fixture and the `jest.requireActual` fix in `utils.test.ts` must land before new utils/main tests are written.
- 002 before 010 on `jest.config.js`: 002 replaces the `@editor` alias with `@views`; 010 only removes `@editor` if it is still present (no-op after 002). Run 002 first.
- 003 before 007: 007 changes the behavior of functions 003 characterizes; 007 updates the 003 tests that assert the buggy behavior.
- 004 before 006: 006's fix is proven by tests written in 004 (the 401-retry test currently cannot pass).
- 013 after 007: the frontmatter UI must not be exposed while the serializer corrupts output.
- 005 and 010 both touch `jest.config.js` / `test/` indirectly — see scope boundaries in each plan; run sequentially, not in parallel worktrees.

## Findings considered and rejected

Round 1 (2026-08-09):

- COR-02 (no network timeouts): not worth doing now — `requestUrl` cannot be aborted; a `Promise.race` wrapper risks double-save on retry paths and the 401 retry fix (006) interacts. Revisit after 006 lands if hangs are observed in the wild.
- SEC-01 (settings path validation): settings are user-controlled; the user attacks their own vault. Fix only if a shared-settings-snippet scenario materializes. Low value.
- SEC-02 (image download content-type/size validation): vector requires compromised CDN/TLS interception; LOW likelihood. Deferred.
- COR-07 (mkdir check-then-act race): narrow window, silent fallback already exists (returns ''). Deferred. (Round 2 upgraded the sibling image-save race to plan 019 — the mkdir variant stays rejected: its failure is caught and degrades to a silent-missing-cover, same fallback as before.)
- ARCH-02/ARCH-03 (two date engines): MED risk user-facing syntax; needs user decision on which syntax wins. Deferred; do NOT unify without asking. (Round 2 found a distinct bug in each engine — plans 016 and 017 fix both without unifying.)
- DIR-04 (search result cache): polish; IGDB free tier 4 req/s not exceeded by the notes workflow. Deferred.

Round 2 (2026-08-09):

- COR-02 (no network timeouts) — **re-prioritized: now planned as 021** (user-approved 2026-08-09); the earlier rejection rationale (interaction with 401-retry) was resolved by 006's ApiError contract.
- Finding R2-9 (known-key strip mangles literal {{field}} text inside game-data values): edge case, LOW impact; the strip pass is deliberately narrower than the old `{{word}}` strip. Not worth a plan.
- Finding R2-10 (settings toggle re-render loses textarea focus): no data loss (per-keystroke save); focus annoyance only. Not worth a plan.
- DIR-R2-1 (i18n full implementation from spike doc): **planned as 022** (user-approved 2026-08-09).
- DIR-R2-2 (date engine unification): decision on syntax belongs to the user (ARCH-03); plans 016+017 fix both engines' bugs without it. Still deferred.
- DIR-R2-3 (E2E CDP harness): **planned as 023** (user-approved 2026-08-09).
- R2-process note: the round-2 audit subagent wrote plans 016-020 and index edits without authorization. Content was fully re-vetted by the advisor (excerpts verbatim-verified against live code, all bugs independently reproduced) and user-approved; the deviation is recorded here for the record.

Round 2 default selection (no interactive user available): top 5 by leverage — plans 016-020. Recorded here per the improve skill's non-interactive rule.
